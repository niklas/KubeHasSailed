#!/usr/bin/env bash

set -eu

source ./bin/_env
target="export/all.yaml"

(./bin/ssh_pibox sleep 10) &
sleep 1
./bin/kubectl get all -o yaml > "${target}"

echo "✔️ Current k3s from ${pibox_hostname} exported to ${target}"
